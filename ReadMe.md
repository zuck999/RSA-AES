# ▶ How to Run (Terminal)
node sender.js


Copy:

Encrypted AES Key

Encrypted Message

Paste them into receiver.js

node receiver.js


# ✅ Output:

Decrypted Message: HELLO FROM SENDER

# 🧠 Concept Summary (For Viva)

“We used hybrid encryption.
RSA is used for secure exchange of the AES key.
AES is used for encrypting chat messages efficiently.
The server only forwards encrypted data and cannot read messages.”

# ⚠️ Important Note (Write in Report)
This project uses simplified cryptographic algorithms
for educational purposes only. In real applications,
standard libraries and TLS are used.;'